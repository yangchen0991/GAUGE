# 运行时生成物（可由源码重新生成）
app.log
smoke_result.json
monitor.ico
__pycache__/
*.pyc

# 数据快照成品（refresh.py + template.html 可重新生成，含活库快照不入库）
AI-Agent监控台.html
AI-Agent监控台.data.json
# 崩溃窗口残留的原子写临时文件（tmp + os.replace 中断产物）
AI-Agent监控台.data.json.tmp
AI-Agent监控台.html.tmp
brand/
desktop/widget.json
desktop/build/
desktop/dist/
desktop/__pycache__/
desktop/monitor/__pycache__/
AI-Agent监控台.exe

# PyInstaller 构建产物（仓库根执行 desktop/构建EXE.bat 生成；与 desktop/build|dist 条目并存）
build/
dist/
# 会话探针证据与浏览器调试临时目录（本地保留，不入库）
output/
.playwright-cli/

# 本地 Agent 记忆与任务数据（不入库）
.workbuddy-ai/

# Inno Setup 构建产物与构建日志（installer/GAUGE衡.iss 可重新生成）
installer/out/

# ===== ↑ 以上同步自 .gitignore（「从 .gitignore 同步」只重写以上部分）=====
.git/
.hg/
.svn/
node_modules/
bower_components/
jspm_packages/
site-packages/
venv/
coverage/
htmlcov/
lcov-report/
cmakefiles/
cmake-build-*/
bazel-*/
pods/
deriveddata/
storybook-static/
playwright-report/
test-results/
allure-results/
allure-report/
cdk.out/
*.egg-info/
*.dist-info/
eggs/
pip-wheel-metadata/
wheels/
# ----- ↑ 以上为 ZCode 默认排除规则（自定义规则请写在本行下方，不会被同步/恢复改动）-----
# 自定义规则写在下方（本行提示可删除）
