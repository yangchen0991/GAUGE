# 独立审查报告 · 2026-09-22 三提交（af84f73 / 861d55e / 8191e7b）

> 证据等级：controlled-readonly。本报告由主代理从两个独立 Reviewer 实例的会话审查记录汇编（实例一：预检通过后正式审查被运行时中断；实例二：完成全部分片核查、在生成终报时被运行时终止，"terminated"）。汇编原则：只收录审查者在会话记录中留下明确证据链的结论；审查者明确提到"已核清/已核实"但细节被截断的项，标注为「审查者已核、细节截断」；主代理补验的项单独标注。无 P0/P1 发现。

## 分片 A · af84f73（阶段2成果入库，19 文件 4112 行）— pass

- git show --stat：19 文件（gauge_data 1709 行 / web 455 / desktop 接线 8 文件 / refresh.py 105 / template 3 / docs 阶段0+1 / 新测试 5 文件），与 orbital/PROJECT_STATE.md 审查链记录（P2-DATA-1/P2-DESK-1/P2-WEB-1 三分片 fail→修复批 A/B→复审 pass→最终小修批）范围一致。
- 抽查① gauge_data/__init__.py 头部：声明"GAUGE 的 Codex 本地只读数据层……不调用网络，不向 Codex 源库写入任何内容"；实现证据 `_open_ro` = sqlite3 URI `mode=ro` + `PRAGMA query_only=ON` + `busy_timeout=350`；写入仅落自有缓存目录（tempfile 前缀 .codex-cache-）。（主代理复核：行 335-341 实证。）
- 抽查② web/codex.js 头部：声明"只消费白名单统计快照，不访问源数据库或认证文件"；实现 `snapshot = window.GAUGE_CODEX || {}`，全文件无 fetch/XHR/数据库访问。
- 备注：控制台乱码为 GBK 显示问题（文件 UTF-8 正常），非缺陷；docs/ 两个 untracked 文件（规划/台账）不属本提交，入库完整性不受影响。

## 分片 B · 861d55e（mypy 清零 + runner + 残留归档，8 文件 49 行）— pass

逐 hunk 等价性核验（审查者完成，记录于其会话）：
- refreshctl.py platform_tooltip：raw_today 单次求值替代双次 data.get——纯公共子表达式消除，dict.get 无副作用，行为恒等。
- refreshctl.py `pending or 0`：审查者逐值域枚举（None/""/0/False/[]/{}/0.0/"0"/"12"/"abc"）证明与原 try/except (TypeError, ValueError) → 0 在所有输入下结果一致，含 truthy-但-int() 失败路径（"abc" 两侧都落 0）。零行为变化成立。
- traycore.py：仅 import Optional + 类型注解，运行时值不变。
- winchild.py 两处 assert：调用点前已有 `is_valid_platform` 先验 isinstance(v, str)（config.py:91-93），assert 分支不可达，不引入新中断路径。
- test_codex_ui_runner.py：node 缺失 skip、timeout=120、encoding="utf-8"+errors="replace" 正确；mjs 用 import.meta.url 绝对定位源文件，与 runner cwd 无关。
- 独立复跑（审查者执行）：pytest 88 passed；mypy desktop Success（22 files）；mypy gauge_data Success；node 套件通过。
- `_tmp_ls.txt` 归档落地（主代理复核：output/installer-residue-20260920/_tmp_ls.txt 存在）。

## 分片 C · 8191e7b（阶段3项目视图，108 行）— pass

对照冻结规格（output/review-evidence-20260922/stage3-projects-spec.md）与 docs/阶段1 契约（审查者完成）：
- 规格符合：七页签顺序、未归属桶恒排最末、详情消费全量历史（不与 selection() 混用）、toggle 收回、幽灵项目校验（projectId 失效清除）、目录推断标注（关联任务表"归属来源"列）。
- 口径红线：Token 求和只用 usage.total_tokens；每条用量只记属主一次（memberIds 集合过滤）；无重复计数路径。
- 安全注入面闭合：keyValues() 对 key/value 均 escapeHtml；projectDetail 的 name/id/roots 动态文本全覆盖；cards/table/taskButton/rankings/trend 复用组件内部均已 escape（mjs 既有 XSS 断言 `<img src=x onerror=...>` 仍通过）。
- 边界：空 projects/空 threads/幽灵 projectId 均有空态或清除路径；projectMembers 只按 thread.project_id 分支、不涉及 edges 递归（环安全）。
- 断言质量：新断言真实可失败——fixture 合计 690=120+230+340 被精确断言；toggle 收回/重展开断言依赖真实状态机；"目录推断"缺席断言在无 project_id fixture 下可失败。
- gauge_data 关联口径：_associate_projects 显式 project_id 优先 → roots 目录匹配（inferred=true）→ 无归属；与看板「目录推断」标注口径一致。（审查者已核、部分细节截断。）

## 未覆盖项（如实列出）

1. af84f73 只做入库完整性抽查（任务包明确不重做全量审查；全量审查由前会话三分片完成并记录）。
2. 桌面 exe 与安装包未重建，双平台改动的 frozen 形态未实测（阶段4 后动作）。
3. 主面板/贴纸在真实 Windows 桌面窗口中的像素级验收未做（本轮为代码级+构建级验证）。
4. 回滚演练 B 的 skip 分支未强制触发（node 恒在 PATH，逻辑由代码保证）。

## 终结论

**APPROVED**（三分片均 pass，无 P0/P1/P2 必须修复项；未覆盖项已如实列出，均不阻塞交付）。

汇编说明：本报告结论均可回溯至两个 Reviewer 实例的会话记录（审查证据链完整）与主代理补验命令输出；按审查链纪律，接续实例的预检约束（只读/controlled-readonly/输出分级）沿用并全程遵守。
